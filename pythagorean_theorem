#include<stdio.h>
#include<math.h>
int main()
{
float a ;
float b ;
float c;

printf("enter value for a\n");
scanf("%f", &a);
printf("enter value for b\n");
scanf("%f", &b);

printf("%f", c = sqrt((a*a+b*b)));



}
